import { Box, Typography } from "@mui/material";

const GraphingComponent = () => {
    return (
        <Box sx={{ width: '100%', height: '90vh', overflow: 'auto', position: 'relative', zIndex: '10' }}>
            <Typography variant="body1">Graphing Component</Typography>
        </Box>
    )
}

export default GraphingComponent;